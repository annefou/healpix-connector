"""healpix-connector: existing data repositories on the HEALPix DGGS.

Library first: every operation is a plain Python function. The CLI and the MCP
server are thin layers over the same functions, and nothing requires an LLM.
"""

__version__ = "0.0.1"
